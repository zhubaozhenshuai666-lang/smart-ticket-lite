<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zewbby.smartticket.mapper.StockCompensationRecordMapper">

    <insert id="insert"
            parameterType="com.zewbby.smartticket.domain.entity.StockCompensationRecord"
            useGeneratedKeys="true"
            keyProperty="id">
        INSERT INTO stock_compensation_record (
            ticket_category_id,
            request_id,
            consistency_record_id,
            compensation_type,
            before_redis_stock,
            after_redis_stock,
            mysql_available_stock,
            in_flight_deducted_quantity,
            expected_redis_available_stock,
            delta,
            status,
            result_message,
            created_at,
            updated_at
        ) VALUES (
            #{ticketCategoryId},
            #{requestId},
            #{consistencyRecordId},
            #{compensationType},
            #{beforeRedisStock},
            #{afterRedisStock},
            #{mysqlAvailableStock},
            #{inFlightDeductedQuantity},
            #{expectedRedisAvailableStock},
            #{delta},
            #{status},
            #{resultMessage},
            #{createdAt},
            #{updatedAt}
        )
    </insert>

    <select id="countByStatus" resultType="java.lang.Long">
        SELECT COUNT(1)
        FROM stock_compensation_record
        WHERE status = #{status}
    </select>
</mapper>
