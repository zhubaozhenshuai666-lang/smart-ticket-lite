<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zewbby.smartticket.mapper.LocalMessageMapper">

    <resultMap id="LocalMessageResultMap" type="com.zewbby.smartticket.domain.entity.LocalMessage">
        <id property="id" column="id"/>
        <result property="messageId" column="message_id"/>
        <result property="businessType" column="business_type"/>
        <result property="businessKey" column="business_key"/>
        <result property="exchangeName" column="exchange_name"/>
        <result property="routingKey" column="routing_key"/>
        <result property="payload" column="payload"/>
        <result property="status" column="status"/>
        <result property="retryCount" column="retry_count"/>
        <result property="maxRetryCount" column="max_retry_count"/>
        <result property="nextRetryTime" column="next_retry_time"/>
        <result property="lastError" column="last_error"/>
        <result property="sentAt" column="sent_at"/>
        <result property="confirmedAt" column="confirmed_at"/>
        <result property="returnedAt" column="returned_at"/>
        <result property="deadAt" column="dead_at"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="BaseColumns">
        id,
        message_id,
        business_type,
        business_key,
        exchange_name,
        routing_key,
        payload,
        status,
        retry_count,
        max_retry_count,
        next_retry_time,
        last_error,
        sent_at,
        confirmed_at,
        returned_at,
        dead_at,
        created_at,
        updated_at
    </sql>

    <insert id="insert"
            parameterType="com.zewbby.smartticket.domain.entity.LocalMessage"
            useGeneratedKeys="true"
            keyProperty="id">
        INSERT INTO local_message (
            message_id,
            business_type,
            business_key,
            exchange_name,
            routing_key,
            payload,
            status,
            retry_count,
            max_retry_count,
            next_retry_time,
            last_error,
            sent_at,
            confirmed_at,
            returned_at,
            dead_at,
            created_at,
            updated_at
        ) VALUES (
            #{messageId},
            #{businessType},
            #{businessKey},
            #{exchangeName},
            #{routingKey},
            #{payload},
            #{status},
            #{retryCount},
            #{maxRetryCount},
            #{nextRetryTime},
            #{lastError},
            #{sentAt},
            #{confirmedAt},
            #{returnedAt},
            #{deadAt},
            #{createdAt},
            #{updatedAt}
        )
    </insert>

    <select id="selectByMessageId" resultMap="LocalMessageResultMap">
        SELECT
            <include refid="BaseColumns"/>
        FROM local_message
        WHERE message_id = #{messageId}
    </select>

    <select id="selectPublishableMessages" resultMap="LocalMessageResultMap">
        SELECT
            <include refid="BaseColumns"/>
        FROM local_message
        WHERE status IN ('INIT', 'FAILED')
          AND retry_count &lt; max_retry_count
          AND (next_retry_time IS NULL OR next_retry_time &lt;= #{now})
        ORDER BY created_at ASC
        LIMIT #{limit}
    </select>

    <update id="tryMarkSending">
        UPDATE local_message
        SET
            status = 'SENDING',
            updated_at = NOW()
        WHERE id = #{id}
          AND status IN ('INIT', 'FAILED')
          AND retry_count &lt; max_retry_count
          AND (next_retry_time IS NULL OR next_retry_time &lt;= NOW())
    </update>

    <update id="markSent">
        UPDATE local_message
        SET
            status = 'SENT',
            sent_at = #{sentAt},
            last_error = NULL,
            updated_at = NOW()
        WHERE id = #{id}
          AND status = 'SENDING'
    </update>

    <update id="markConfirmed">
        UPDATE local_message
        SET
            status = 'CONFIRMED',
            confirmed_at = #{confirmedAt},
            last_error = NULL,
            updated_at = NOW()
        WHERE message_id = #{messageId}
          AND status IN ('SENDING', 'SENT')
    </update>

    <update id="markPublishFailedById">
        UPDATE local_message
        SET
            retry_count = retry_count + 1,
            status = CASE
                WHEN retry_count + 1 &gt;= max_retry_count THEN 'DEAD'
                ELSE 'FAILED'
            END,
            next_retry_time = CASE
                WHEN retry_count + 1 &gt;= max_retry_count THEN NULL
                ELSE #{nextRetryTime}
            END,
            dead_at = CASE
                WHEN retry_count + 1 &gt;= max_retry_count THEN #{deadAt}
                ELSE dead_at
            END,
            last_error = #{lastError},
            updated_at = NOW()
        WHERE id = #{id}
          AND status IN ('SENDING', 'SENT')
    </update>

    <update id="markPublishFailedByMessageId">
        UPDATE local_message
        SET
            retry_count = retry_count + 1,
            status = CASE
                WHEN retry_count + 1 &gt;= max_retry_count THEN 'DEAD'
                ELSE 'FAILED'
            END,
            next_retry_time = CASE
                WHEN retry_count + 1 &gt;= max_retry_count THEN NULL
                ELSE #{nextRetryTime}
            END,
            dead_at = CASE
                WHEN retry_count + 1 &gt;= max_retry_count THEN #{deadAt}
                ELSE dead_at
            END,
            returned_at = COALESCE(#{returnedAt}, returned_at),
            last_error = #{lastError},
            updated_at = NOW()
        WHERE message_id = #{messageId}
          AND status IN ('SENDING', 'SENT')
    </update>

    <select id="selectConfirmTimeoutMessages" resultMap="LocalMessageResultMap">
        SELECT
            <include refid="BaseColumns"/>
        FROM local_message
        WHERE status IN ('SENDING', 'SENT')
          AND updated_at &lt;= #{timeoutBefore}
        ORDER BY updated_at ASC
        LIMIT #{limit}
    </select>

    <select id="selectRecent" resultMap="LocalMessageResultMap">
        SELECT
            <include refid="BaseColumns"/>
        FROM local_message
        <where>
            <if test="status != null and status != ''">
                status = #{status}
            </if>
        </where>
        ORDER BY created_at DESC
        LIMIT #{limit}
    </select>

    <select id="countByStatus" resultType="java.lang.Long">
        SELECT COUNT(1)
        FROM local_message
        WHERE status = #{status}
    </select>

    <update id="resetForManualRetry">
        UPDATE local_message
        SET
            status = 'INIT',
            retry_count = 0,
            next_retry_time = NULL,
            last_error = NULL,
            sent_at = NULL,
            confirmed_at = NULL,
            returned_at = NULL,
            dead_at = NULL,
            updated_at = NOW()
        WHERE message_id = #{messageId}
          AND status IN ('FAILED', 'DEAD')
    </update>

    <update id="markDeadByMessageId">
        UPDATE local_message
        SET
            status = 'DEAD',
            last_error = #{lastError},
            next_retry_time = NULL,
            dead_at = #{deadAt},
            updated_at = NOW()
        WHERE message_id = #{messageId}
          AND status IN ('INIT', 'FAILED', 'SENDING', 'SENT')
    </update>
</mapper>
